server:
  port: 8081

spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/banking
    username: banking
    password: banking
    driver-class-name: org.postgresql.Driver

#  config:
#    import:
#      - classpath:ledger-application.yml
#      - classpath:account-application.yml

  flyway:
    enabled: true
    locations:
      - classpath:db/migration
    out-of-order: true
    baseline-on-migrate: true